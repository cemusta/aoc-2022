import { sumOfContainedSections, sumOfOverlappingRanges } from './day4';
import { input4 } from './inputs/input4';

console.log(`sum of contained pairs:    ${sumOfContainedSections(input4)}`);
console.log(`sum of overlapping ranges: ${sumOfOverlappingRanges(input4)}`);
